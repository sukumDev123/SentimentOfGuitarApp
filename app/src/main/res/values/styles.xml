<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBox</item>
        <item name="colorPrimaryDark">@color/colorBox</item>
        <item name="colorAccent">@color/goodComment</item>
    </style>

</resources>
